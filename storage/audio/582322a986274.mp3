
4984511229518442

150105199212265117


2-5
The krpano Javascript-Interface object
高中：6 7  9（政治） 10 11 

+---------+---------+---------+---------+--------+---------+--------+---------+--------+---------+----------+
|         | 数学    | 物理    | 化学    |  英语  | 生物    | 地理   | 政治    | 历史   | 语文    |  科学    |
+=========+=========+=========+=========+========+=========+========+=========+========+=========+==========+
| 小学    |   10    |  11     |  12     | 16     | 13      |  15    | 17      | 18     |   14    |  19      |
+---------+---------+---------+---------+--------+---------+--------+---------+--------+---------+----------+
| 初中    |   20    |  21     |  22     |  26    |  23     |  25    |   27    | 28     | 24      |  29      |
+---------+---------+---------+---------+--------+---------+--------+---------+--------+---------+----------+
| 高中    |   30    |  31     |  32     |   36   |  33     |  35    |   37    | 38     |   34    |  39      |
+---------+---------+---------+---------+--------+---------+--------+---------+--------+---------+----------+
  
语文：24（1003\"":4） 34（0）
物理：21（0）    31（88059/null :1099）
生物：23（0）    33（135983/null:135983）
地理：25（0）    35（68292/null:1132）

进行中或未完成

化学：22（0）    32（128645\"":654）
政治思品：27     37
历史28（0）      38

留下的问题：
knowledge字段为空
2、answer转化成list
3、数据库中含有杂乱的数据

今目标日志管理
http://web.jingoal.com/
企业号：5036805 
管理员帐号：admin@5036805（默认登录帐号）
841369840@qq.com （绑定后也可作为登录帐号使用）

2015.3.2
计划任务：

1.数据库的结构统一
2.看看李勋的提取效果
3.着手提取

不懂的地方：
1.133 text = lambda text: isinstancd(text,Comment
原始图片的路径是怎么定义的，另一个文件MvCenturyPIc.py写着

2015.3.2
1.插入数据库，看看语文效果
2.修改格式，查看不妥的地方

1.语文需要修改的地方，修改li标签
2.下划线
3.图片的路径
4.抓取需要下载的图片




语文：
单选题的拆分
/tmp/chinese.txt



2015.3.5
语文有待解决的问题
1、题号删除     yes
2.答案没有提取  yes

1、抓取其他学科图片的链接  no
2、在框里调试语文          yes

1、抓取图片的链接：    no
  抓链接（fetch.py chinese.txt） -- 移动图片（不移动小图片，MV.py）-- 检查图片是不是抓取完全() -- 修改数据库中的图片路径

2、实行是否可行，图片地址 yes
2015.3.7
1、解析字段的问题：去掉解析2字
/data/wuwq/21century/century_quest/YzN2Q/YzN2Q__QMT5N.html       考点，点评， 试题分析
/data/wuwq/21century/century_quest/IjN3I/IjN3I__wMT5M.html nothing

2015.3.12

1、加入去掉footer这些页面。
2、去掉来源等词或水印
3、解析政治历史

2015.3.13
1、手机与服务器的互联，lamp搭建，数据库的建立，
2、完成文本的收发功能

2015.3.16
1、修改水印，
2、整合注意的问题：
  是不是本科目
  移动图片
  
2015.3.19
1、图片路径：img.zuoyetong.com.cn/test/
2、测试页面：http://tkdemo.zuoyetong.com.cn/html/test.html

任务：1、 完成数据库备份yes
      2、上传备份脚本yes
      3、移动图片，查看测试结果 yes 复用代码
      4、整理代码，A.剔除不是本科目的，移动图片，去水印（下载数据，去水印，更新数据）no
2015.3.20 
任务：1、熟悉的代码
      2、完成上面的4

2015.3.21
任务： 1、完成上4


2015.4.1
任务：1、完成微信的客服接入服务。
      2、显示格式，按照ID显示，按照日期降序，回复功能
2015.4.9
geometry：几何学
mesh：网
material：材料
renderer：渲染
php的配置文件 /etc/php5/apache2/php.ini
错误报告文件：/var/log/apache2/error.log
网页路径：/var/www/html
session路径 /var/lib/php5 在php5中session.save_path保存
Ubuntu apache添加端口隐射
 1、/etc/apache2/ports.conf 中添加监听端口Listen：8080
 2、在/etc/apache2/sites-enable/000-default.conf中仿照填写


2015.4.10
animate:活的
prototype:原型
javascript
点击调用函数onclick="function()";
点击弹出onclick="alert('welcome')";
x = document.getElementById("demo");
x.innerHtml("welcome javascript"); 
string.match("string");匹配字符串
string.match(/\d+/g);匹配正则，查找所有的数字
isNaN(s);s是非法数字，返回true
render = new THREE.WebGLRenderer({antialias:true});传递参数，启动抗锯齿渲染
var light = new THREE.DirectionalLight(0xffffff,1.5);创建光源
var material = new THREE.MeshPhongMaterial({map:map});


2015.4.10
groups:产看当前用户的组内成员
groups user:产看user用户所在的组以及组内成员
whoami：查看当前登录用户名

/etc/group文件是用户组的配置文件
gruop_name:passwd:GID:user_list  其中，每个用户使用“，”分隔

1、用户管理
adduser 选项 用户名 
  选项：
  -d 目录 指定用户主目录
  -g 用户组 指定用户所属的用户组
  -G 用户组 指定用户所属的附加组
  -s Shell文件 指定用户的登陆shell
userdel 选项 用户名
usermod 选项 用户名 选项和adduser相同
passwd 选项 用户名
  选项
  -l锁定口令，禁用账号
  -u口令解锁
  -d 使账号乌口岭

2、用户组管理
groupadd 选项 用户组
 选项
 -g GID 
groupdel 用户组 删除用户组
groupmod 选项 用户组
 选项
 -g GID
 -o 与-g一起使用，用户组的新GID可以与系统已有用户组的GID相同
 -n 新用户组 将用户组的名字改为新名字
newgrp root 切换到root的用户组
su root //切换到root操作
su wuwq //切换到wuwq操作
3、与用户账号有关的系统文件
/etc/passwd文件每一行
 用户名:口令:用户标识号:组标识号:注释性描述:主目录:登录shell
 口令：加密后的数据放在了/etc/shadow中
 用户标识号：如果多个用户名对应一个用户标识号，系统内部认为这是一个用户，但他们可以有不同的口令，主目录和登录shell；root为0， 1-99：系统保留，作为管理账号， 普通用户100开始
 组表示号：用户所属的用户组
 注释性描述：记录用户的个人情况
 主目录：用户的起始工作目录
/etc/shadow
 登录名:加密口令:最后一次修改时间:最小时间间隔:最大时间间隔:警告时间:不活动时间:失效时间:标志
/etc/group
组名:口令:组标识号:组内用户列表

4、批量添加用户
 1、编辑文本用户文件user.txt 
    user001:x:601:100:user:/home/user001:/bin/bash
    user002:x:602:100:user:/home/user002:/bin/bash
 2、以root身份执行命令/usr/sbin/newusers：
    命令行输入：
    #newusers < user.txt
    此时查看#vi /etc/passwd 是否写入新用户
 3、取消shaodow功能
    #pwunconv
 4、编辑用户密码passwd.txt
    user001:user001123
    user002:user002123
 5、以root执行命令/usr/sbin/
    #chpasswd < passwd.txt
 6、#pwconv
5、构建ssh服务器
 1、vi /etc/ssh/sshd_config
    
6、配置用户的权限：
 1、给用户配置为管理员权限：
    在# vim /etc/sudoers中修改
      #User privilege specification
      root   ALL=(ALL:ALL) ALL
      wuwq   ALL=(ALL:ALL) ALL
 2、修改文件拥有者和群组
     #chown -R www-data:www-data  ./*
 3、修改文件的属性
 4、创建软链接：
    #ln -s existingfile newfile

chmod [options] mode[,mode] file1 [file2]
 八进制语法：chmod 664 myfile 所有者 用户组 其他用户
 符号模式：who：u(user) g(group) a(all) 
           operator +(add) -(remove) =(reset)
           pemission r(read) w(write) x(execute)
           chmoe ug+x file1
修改命令路径（shell）
临时别名alias rm='~/.conf/rm'
永久别名：在自己的根目录下.bashrc
是文件立即生效 source .bashrc



vim配置
系统配置：etc/
:set paste 保留原粘贴格式：
:set nopaste 退出粘贴模式
:map <F10> :set paste<CR> 设置键盘映射模式
:map <F11> :set nopaste<CR>
:set pastetoggle=<F11>  绑定快捷键



调试颜色方案
加几个按钮，调试颜色
增加灯光，通过点击在某些地方添加灯，是颜色很好看


2015/4/13
js：函数的首字母小写之后每个单词首字母大写
THREE.js 函数的首字母大写，之后每个首字母大写
PerspectiveCamera(fov, aspect, near, far)
 aspect: width/height
2.将相机和网格添加到camera中

灯光的种类：
AmbientLight:对所有物体投射光
AreaLight：
DirectionalLight
PointLight
Spotlight


加入读写文件
858a81
9d85c9
ffffed
abffed
jquery选择器
#id     $("#lastname") id = "lastname"的所有元素
.class  $(".lastname") class = "lastname"的所有元素
element $("p")	       所有<p>元素


object.OpenTextFile(filename[, iomode[, create[, format ]]])
object 必选项，是FileSystemFile的名称
filename 必选，打开的文件名
iomode 可选，1：只读方式打开，2：写方式打开文件 8：文件末尾添加
create 可选，当指定的filename不存在时是否创建
format 可选，TristateTrue 以Unicode格式打开文件；TristateFalse 以Ascii格式打开文件；TristateUseDefault，使用系统默认值打开文件


2015-4-14
需要完成
1、对文件的读写
2、显示的灯光有光泽一些
3、修改键入值读取

新知识：
1、service --status-all,前面会有加号减号，
 +：正在运行
 -：没有运行
 ?：未知状态
2、修改，
 slider的事件监听，slide改成set
 
3、notepad快捷键
 ctrl + alt + f:折叠当前层次
 ctrl + alt + shift + f：展开当前层次
 alt + 0：折叠全部
 alt + shift + 0:展开全部
 alt + D：比较相邻标签页不同
4、javascript给元素添加事件
 a = getElementById("a");
 a.onchange = function(){alert("hello")};
 变量前面需要加var
 jqury查找标签时[0]返回与document.getElementById相同，或者是使用$("#id").val()读取，使用$$("#id").val(value)赋值
 类型转换：Boolean（value）将值转化成Boolean类型
           Number(value)将值转化为数字（整型或浮点数）
           parseInt(value)转化前面的数字部分
	   parseFloat（value）转化前面的小数部分。
 	   String（value）
           a.toString(16),将a转化成16进制
进制转换

公司抬头：北京清显科技有限公司
阿里云服务器控制台：
用户名：tsingvisual@163.com
密码:qingxiankeji123
阿里云服务器登陆
name:root 
passwd:QingXKJ123

公网ip：123.57.60.207
内网ip：10.173.20.218
终端管理密码： 882121
mysql -u root -p tsi20:01 2015/9/30ngvisual

万网域名管理
用户名：tuliao2009@163.com
密码：2014QingBiJi
域名：tsingvisual.com

网站备案系统：
用户名：tsingvisual@163.com
密码：QingXKJ123

图片管理网站
www.quanjin.com
http://imgfave.com/  hellousername 

php使用函数
1、explode(String $delimiter, String $string[,int limit])//拆分函数
    $tmpArray = explode(".", "a.xt");//$tmpArray[0] => a, tmpArray[1] => txt
   implode(String glue, arry $pieces)
2、类似于C语言的指针，要传递参数，需要传入引用。
3、scandir($dir),遍历当前路径下的所有文件，其中要注意“.”和“..”需要跳过
3、is_dir($dir),判断是不是文件或文件夹
4、获取上级目录dirname
  path = "/etc/apache2/conf.ini"
  dirname(path)    /etc/apache2
  dirname(dirname(path))  /etc
5、上传文件 
前端文件upload_test.html
<form action="upload_test.php" method = "post" enctype="multipart/form-data">
<input type = "file" name="file" id="file">
<input type = "submit" name = "submit">
</form>
php文件 upload_test.php
$_FILES["file"]["name"] $_FILES["file"]["type"] $_FILES["file"]["tmp_name"] $_FILES["file"]["type"] $_FILES["file"]["name"] $_FILES["file"]["size"] 
移动文件move_uploaded_file
6、解压文件zip
 $zip = new Archive();
 $res = $zip->open("1.txt");
 if($res === True){
   $zip->extractTo("extract/1");
  }
 $zip->close();
 


2015-4-16
jquery插件
1、jquery.shCircleLoader-min.js 加载圈

Three.js
ColladaLoader：
 methods：
   .load(url, onLoad, onProgress)


git常用命令总结
配置文件
/etc/gitconfig :对系统所有用户都适用
~/.gitconfig: 对当前用户有效
.git/config:只对当前项目有效

配置用户信息
git config --global user.name "wuwq"
git config --global user.email "wuwq@example.com"
 使用--global将配置信息写在~/.gitconfig中
 使用--system将配置写在/etc/gitconfig
 去掉--global将配置写在.git/config

配置编辑器
git config --global core.editor vim

差异分析工具
git config --global merge.tool vimdiff

配置缩写
git config --global alias.co checkout
git config --global alias.st status
git config --global alias.ci commit
git config --global alias.br branch

git add <file_name>
git commit
git checkout [<branch_name>]
git push origin <branch_name>
分支操作：
 git checkout -b dev 创建并切换
 git branch 查看分支
 git checkout master切换到master上
git init 把当前的目录变成git仓库
git clone 把远端文件克隆岛本地
git add  将文件添加到仓库
git commit 提交文件

git使用增加缓存

git config --global http.postBuffer 157286400
   2  8 14 16 18 19 20 22 24
git log查看commit日志
一行显示 git log --pretty=oneline

git reset --hard commit_id回退到提交的id
也可以使用commit_id也可以使用HEAD代替，HEAD 表示当前版本，HEAD^表示前一个，HEAD^^表是前两个
git reflog 记录每次输入的命令


tar 压缩解压
tar -zcvf a.tgz a.log b.log   打包并以gzip压缩，以a.tgz或者是a.tar.gz命名
tar -ztvf a.tgz 查看压缩包中有哪些文件
tar -zxvf a.tgz 解压文件
tar -zxvf a.tgz a.log 部分解压文件
tar -zcvpf -zxvpf 保留源文件权限

unzip解压文件：
 unzip test.zip 将文件解压到当前目录
 unzip -n test.zip -d /temp 将文件解压到/tmp文件夹下，如果文件存在不覆盖文件
 unzip -o test.zip -d /temp 将文件解压到/tmp文件夹下，如果文件存在覆盖文件
 unzip -v test.zip 查看压缩文件目录，但不解压
 unzip -O GBK test.zip 在window上压缩的文件解压时指定解压字符集，避免出现中文乱码的情况

rendering engine:

-webkit:chrome,Safari
-moz:Firefox
-o:Opera
-ms:IE

问题：google-chrome在鼠标移动的时候，相机的方位没有转换，
解决：将屏幕的分辨率调高，就没有问题了。

20145-4-17
php和JavaScript的区别

变量：
$tmp		var tmp
函数：
		从第二个单词开始首字母大写
数组添加：
a[] = newitem	a.push(newitem)


2014-4-18
php语法：
1、=== 判断类型和值都相等
   == 判断值相等
2、查看php 的GD库是否安装
  php5 -m|grep -i gd
    -i ignore case
    -m show compiled in modules


mysql
1、修改自增：alter table table_names auto_increment = 1
2、查看所有信息：show full columns from table_name

微信
答辩总结
 中期：老师不是很满意
   1、ppt中的展示效果不好，没有实物图展示，或者是实时演示（推荐前者）
   2、加入功能
图片获取方式GET
identify 
0：管理员
1：老师
2：学生
3：老师待通过

use_flag 
0: the message is sent by student to teacher;
1: the message is sent by teacher to student;

read:
0:message is not read default
1:message is read



菜单id：
查看：
  已选课程：check_chosen
  我的创建：check_created
加入：
  已开课程：join_existed
  创建课程：join_create
其他：
  绑定：other_binding
  反馈：feedback
  解除绑定：other_unbinding

表设计：
所有首字母都小写
user
  userid   int primary key auto
  openid   varchar(64)
  username varchar(32)
  password varchar(32)
  portrait varchar(64)
  email    varchar(32)


choose
  chooseid  primary key
  userid    foreign key(userid)
  courseid  foreign key(couserid)
  time
  valid 1：在班级中，0已经退出班级

message
 messageid  primary key auto_increment
 courseid   foreign key
 userid     foreign key
 scanTimes   int 
 remarkTimes int
 level      tinyint 0：发帖人 1:评论的人
 time date
 latestremark timestamp

 
course
  courseid  primary key
  coursename 
  startTime  
  endTime
  authorid(userid)
  author(username)
  profile
  schedule array
  

  Time

页面设计  
  所选课程: 
    课程列表 courseName autor button（check）  
  所有课程：
    课程列表 courseName autor button (check join)
  创建课程：
     coursename startTime endTime profile calendar
通知 课程表(上课时间用深色显示) 讨论 统计信息（时间段登陆次数，发帖总数） 退出课程
  绑定
  反馈

http://buyfang.cn/weiphp2.0.1202/index.php?s=/home/weixin/index/id/107.html
weiphp

http://182.92.116.6/echo_server.php
echo_server

1、收不到微信后台的数据：
  1、在url开始多了一个空格，没有信息返回
  2、json数据格式错误，使用在线验证 http://www.kjson.com/
    json是由对象和数组组成
        对象：由无序“名称/值”对组成，由分号隔开{"name":"value"}
        数组：是value的有序集合，使用逗号分隔,[]包围
2、微信token验证接口配置，需要在函数写在当前文件下。     

vim查找替换
:%s/\<four\>/4/gc
 %:所有行
 \<: 匹配单词开头
 \>: 匹配单词结尾
 g:  匹配行中所有匹配点
 c:  替换时询问
 
2015-4-21
1、切换窗口：ctr + tab
2、双窗口：ctr + W

2015-4-22
controls = new THREE.TrackballControls(camera);//将控制器加载到照相机上，如果
 //将光加到照相机上，那么看到的物体被照亮，如果光加到场景上，那么场景光的位置不动
mysql显示全局变量：
 mysql操作
 show global variables like "wait_timeout"; //连接超时。
 show processlist;
 set global wait_timeout = 1; //
 linux控制台
 mysqladmin -uwuwq -p processlist
 
2015-4-23
1、配置VPN
2、Native Client software development kit（SDK）


配置环境变量
NACL_SDK_ROOT ：H:\3d\nacl_sdk\sdk_cache\pepper_37\
CHROME_PATH：   C:\Users\Administrator\AppData\Local\Google\Chrome\Application\chrome.exe

修改install.py
DEFAULT_VS_DIRECTORIES = ['d:\\documents\\visual studio 2010',
                           'd:\\documents\\Visual Studio 2012'] 
配置环境变量：
dos窗口： 
 set path=c,重启dos
 echo %PATH% 环境变量生效
 d:/ 回车， 进入D盘：
 dir        列数当前目录的文件和文件夹
 dir /?     查看命令选项
 dir /a     列出所有文件和隐藏文件迟到
 cd 进入某一文件夹
 md 建立文件夹
 rd 删除文件夹
 cls 清除屏幕
 move filename destinationfile
 del 删除文件，不能删除文件夹
 deltree 删除文件夹和他下面所有文件和文件夹
 type 查看文件内容
 format x:  用fat文件系统格式化
dos网络相关命令：
 ping
 net
 netstat
 nbtstat
 tracert
 pathping
 ftp
 telnet
 ipconfig
   /all 显示详细信息
   /release 释放当前ip
   /renew 重新申请ip 
   /flushdns刷新dns缓存
   /registerdns 重新在dns上注册自己
 arp
 nslookup


javasritp/html -> nativeclientModule发送消息
 加载完native client后。通过helloworldModule=getElementById("hello_word")获取句柄
 helloworldModule->PostMessage()向NativeClient发送消息
javasritp/html 接收 nativeclientModule消息
 listener.addEventListener('message', handleMessage, true)
nativeclientModule向javasritp/html发送消息
 在nativeClient端，以一个pp::instance()来代表javasritp/html实体，所以，以pp::instance()->postMassage()发送消息
nativeclientModule接收javasritp/html消息
 pp::Instance() 的handleMassage()接收消息
 
 
lubuntu虚拟机
用户名：wuwq
密码：wuwq123
ftp:用户名和密码
用户名：wuwqftp
密码：wuwqftp123
目录：/home/wuwq/beta/wordpress/wp-content
用户名：model


网易：
hellousername@163.com
hellopassword

wordpress
subscribe:123
author:123
admin:wuwq123




2015-4-29
1、使用wordpress
2、微信后台将页面布局搭好，建立数据库

wordpress:
1、主题：/wp-content/themes/ 创建文件夹
  添加3个文件：index.php functions.php style.css 
page：静态
post：动态日志类型
回车：下一段
shift+回车：下一行
plugin:
 wp-pagenavi  添加翻页功能
 my page order 页面排序插件
 my category order 分类排序插件
自带自动存档功能
 delete revision 删除冗余版本
 revision control 禁用或控制自动存档
3、搬家
 压缩文件和数据库
 将压缩文件上传到新服务器上，建立数据库，导入表
 修改配置文件wp-config.php
 修改表wp-options 字段siteurl 和 home
相关函数：
 filter
 wp_insert_post_data ：
 wp_prepare_attachment_for_js 

主题制作：
主页：
 先找home.php，没有再找index.php
文章页：
 1、single-{post_type}.php
 2、single.php
 3、index.php
页面
 1、自定义模板
 2、page-{slug}.php如果页面的缩略名是news，wordpress将会查找page-news.php
 3、page-{id}.php
 4、page.php
 5、index.php
分类
 1、category-{slug}.php如果分类的缩略名是news，wordpress将会查找category-news.php
 2、category-{id}.php
 3、catego.php
 4、archive.php
 5、index.php
标签
 1、tag-{slug}.php
 2、tag-{id}.php
 3、tag.php
 4、archive.php
 5、index.php
作者
 1、author-{nickname}.php
 2、author-{id}.php
 3、author.php
 4、archive.php
 4、index.php
日期页面
 1、date.php
 2、archive.php
 3、index.php
搜索结果
 1、serch.php
 2、index.php
404页面
 1、404.php
 2、index.php
附件页面
 1、MIME_type.php 可以是任何MIME类型（image.php,viedo.php,audio.php,application.php或其他）
 2、attachment.php
 3、single.php
 4、index.php
函数
bloginfo("name（网站名称）,stylesheet_url（style.css绝对路径） ..." template_url(模板根目录))显示博客的信息
wp_title("sep", true, "right")
功能
ping当自己的网站发表新文章时，通知一些服务器过来爬取
pingback 当自己引用别人的文章发表时，以评论的方式显示在对方的文章下面
get_header() 从当前主题文件夹搜寻header.php
is_home, is_single, is_category 判断标签
get_option("home") 返回博客首页地址
wp_list_categories() 列出博客的分类页
wp_list_pages()列出博客的页面
下面都应该在loop中
the_permalink();输出文章链接
the_title();输出文章标题 wp_query
the_excerpt();输出文章摘录
the_content()输出全文
the_tags($before,$sep,$after)
comments_popup_link("0","1","% so far","css for the link","功能关闭")
query_posts(array("posts-type"=>array("posts","model")));
  显示特定的属性



wordpress修改内容：
index.php显示缩略图
single.page 加入链接（数据库）

名词
 feed：聚合内容，就是读者订阅喜欢的内容
 pingback



php使用函数
date("Y-m-d H-i-s")按格式显示日期和时间
mixed filter_var($ )
<?php
 //反爬虫如果用用户是直接访问comments.php，一般来说不正常
    if (isset($_SERVER['SCRIPT_FILENAME']) && 'comments.php' == basename($_SERVER['SCRIPT_FILENAME']))
        die ('Please do not load this page directly. Thanks!');
?>
time()获取时间戳

php的变量范围
全局变量：函数内使用外部变量时需要声明global；
静态变量：在函数中声明，只有初始时被赋值，声明static

下载文件：wget
$wget -i inputfile   下载inputfile中的所有连接
$wget -d buyfang.cn  下载输出信息

2、mysql增加用户，授权
忘记root密码
 进入命令行
 $sudo vi /etc/mysql/my.cnf //在mysqld添加skip-grant-tables
 service mysqld restart
 进入mysql
 use mysql
 update user set password = password("new-password") where user = 'root'
 insert into user（Host, User, Password）values("localhost", "root", "password");
 flush privileges;
 exit
 service mysqld restart
 root登陆
 grant all privileges on *.* to wuwq@"%";
 
查看linux版本：lsb_release -a
查看软件执行路径 which python
ubuntu安装vsftp

1、sudo apt-get install vsftp -y //-y表示所有的询问都是同意
2、添加ftp账号和目录 nologin的位置：/usr/bin/nologin(ubuntu) 或者是 /sbin/nologin
 useradd -d /home/wuwq/ftp -s /usr/sbin/nologin wuwqftp 设置登录目录和登录shell
 passwd pwftp 设置密码
 chown -R wuwqftp:wuwqftp /home/wuwq/ftp
 将用户添加到组中：usermod -a -G www-data wuwq

3、配置vi /etc/vsftp.conf
 禁止匿名登录
 anonymous_enable = NO
 local_enable = YES
 write_enable = YES
 chroot_local_user = NO 只允许用户访问自己的根目录，禁止用户访问上级目录
 chroot_local_enable = YES
 chroot_list_file=/etc/vsftpd.chrootlist //chroot_local_user设置为NO，这个列表就是所有只能访问自己根目录的用户列表，此时汇报一个错误“Error: 500 OOPS: vsftpd: refusing to run with writable root inside chroot() [SOLVED]”，
    解决的办法是去掉写权限 即chmod a-w file
 allow_writeable_chroot=YES允许写入到该目录下
 file_open_mod=0755  0表示10进制
 local_umask=022  用户的权限，减去该值
 编辑/etc/vsftpd.chrootlist文件，将ftp的账户名添加进去
5/修改shell配置
  vi /etc/shellsvi 
  追加 /usr/sbin/nologin
6、重启vsftp
 service vsftpd restart

添加
 ftp客户端推荐使用filezilla，不要使用8uftp
 主机:123.57.60.207 
 端口号：21
 用户名models
 密码models123
 现在模型文件的根目录是/home/wuwq/beta/viewer/models 若要修改目录需要2步(管理员才有权限)：1、/etc/passwd中“models:x:1003:1003::/home/wuwq/beta/viewer/models:/usr/sbin/nologin”修改目录 2、重启服务：#service vsftpd restart
 测试地址123.57.60.207/wuwq/beta/viewer/viewer.php?name=name&dir=dir




css和html
display:block(or inline， or none，list-item，table，inline-block，flex)
 block:块级元素 新开一行，沾满容器div,p,form H5:header,footer,section
 inline:在段落中加入文字，但不会打乱布局span,a
 none:隐藏元素，同时不保留元素本该显示的空间
   对比visibility:hidden会保留显示空间
 inline-block:创建很多网格充满浏览器
max-width:600px浏览器更好的处理小窗口的情况
margin：0 auto左右外边距
 auto：剩余宽度一分为二显示，即实际效果就是剧中显示
border：边框宽度
padding：内部填充
border，padding都会增加显示的宽度

强制不换行
white-space:nowrap;
自动换行
word-wrap:break-word;
word-break:normal;

idoubi.sinaapp.com
所以现在的方法：
*{
-webkit-box-sizing:border-box;
   -moz-box-sizing:border-box;
        box-sizing:border-box;
}

1、#main{}
   id = "main"
2、.btn{}
   class = "btn"
3、body{}
   <body> </body>
position:static, relative, fixed, absolute, 
 relative: 设置top，right，buttom，left使其偏离正常位置，其他元素不会填充其空隙
 fixed：相对于视窗位置，
 absolute:相对于父元素定位
float:文字环绕图片
 img{
   float: right;
   margin: 0,0,1em, 1em
   width:
   max-width:
 }



clear:left 清除上一个元素的向左浮动属性
媒体查询：
@media screen and(min-width:600px){
 设置css属性
 }
@media screnn and(max-width:599px){
 设置css属性
 }


overflow:auto 如果图片超出容器的大小，可以在容器上加一个清除float属性的值

浮脚筋：
1、足背伸直屈伸动作，踏踏脚动动脚趾头
2、坐凳子上是双腿不停抖动
3、加强体育锻炼
4、抬高脚超过心脏10-15分钟，睡觉前垫高下肢

2015-5-5
"<iframe src="about blank"/>"
1、grep 查找 grep -R content file
   在file中递归的查找content
  -A num :输出匹配行的之后几行
  -B num :输出匹配行的之前几行
  -i :忽略大小写
  -n ：显示行号
  -R ：递归查找
  -v ：输出，没有匹配的行
 管道操作 
 grep apple fruitlist.txt | grep -v pineapple > apple.txt 将查找的内容重定向到文件中

2015-5-9
1、<a href="" target=""> </a> target 用法
  view_window 打开新的窗口, 如果该窗口未关闭，则依然使用原来的窗口
  view_frame 在frame中打开链接
  _blank   浏览器总在新的为命名的窗口
  _self    源窗口打开文档
  _parent  父窗口
  _top     

2015-5-18
mysql
查看索引 show index from table_name 
         show keys from table_name
删除索引 alter table drop index index_name；

添加索引 alter table table_name add unique(column_list)
         alter table table_name add primary key(colun_list)
         alter table table_name add index index_name(colun_list)
  

【盲答辩】通知学生在5月21日12:00前提交（要求所有文件都将导师信息隐去，如果发现有导师信息，直接不及格）：1.纸版论文（活页夹住即可，任务书和论文正文必须有，其他的可不提交）；2.论文电子版（需要任务书和论文正文）；3.答辩决议书（一份）、成绩登记表（一式三份），表头部分导师信息隐去，但是导师正常给成绩、意见和签字；4.《本科毕业设计（论文）成绩评分表（指导教师用表）》，这个表不需要隐去导师信息，正常提交即可；5.答辩PPT，所有的文件都以如下格式命名：论文-学号-姓名；PPT-学号-姓名
  
纸质论文：任务书、论文正文
论文电子版：
答辩决议书（一份）、成绩登记表（三份）
本科毕业设计（论文）成绩评分表（指导教师用表）》
答辩ppt


js html dom
change html style
document.getElementById(Id).style.property=new style
event
<iframe height=498 width=510 src="http://player.youku.com/embed/XOTU4NDk2OTMy" frameborder=0 allowfullscreen></iframe>
<div id="bottom_bar_<?php echo$post_id;?>" style="width:100%;height:30px;background:#f8f8f8;opacity:0.2;displ
ay:none;position:relative;top:-28px;left:0;" onmouseover="bottom_show('bottom_bar_<?php echo$post_id;?>')">              
            <img style="float:right; width:40px;height:26px;margin:1px 5px;" src="<?php echo $img;?>fullscreen.png" oncli
ck="fullscreenmy('<?php echo$post_id;?>')"></img>           
            </div>

<iframe style='width:250px; height:250px;' src='http://123.57.60.207/wuwq/wordpress/view/view-model.php?dir=models/277/5552f2d2845f9/monster.dae' frameborder=0></iframe>

2015-5-20
将微信公众账号中用户的openid与系统用户绑定
1、建立openid 与userid对绑定表，初始时为空
2、用户发送消息，服务器获取用户的openid，
3、服务器生出带3个参数的url，将链接发送给用户
  openid
  timestamp时间戳
  signature签名
   signature签名的生成方法：将openid timestamp token(只有自己知道)字典排序，sha1加密
4、用户点击点击链接，将打开绑定界面，并会将3个参数传递给服务器
5、服务器收到连接请求后，可以通过GET方式获取3个参数；先对signature验证（将openid timestamp token字典排序，sha1加密，生成的signature和GET获取的signature作对比，相同则通过），在对timestamp验证。都通过既可以绑定openid和userid。然后就可以将uerid设置在session中。然后可以渲染验证页面。


页面展示效果
1、页面不能全屏展示，
2、修正title
时间安排
5点写论文结束
6点配图结束
8点格式结束，
9点ppt结束
10点所有文件准备完毕
之后找老师交材料
未解决的问题
1、图片的路径
2、添加通知


2015-5-23
session
1、session设置
session_start();  //打开session文件，每一个浏览器进程自动新建一个session文件，
$_SESSION['admin'] = true; //设置session数据
echo session_id(); //设置或者是输出session_id

手机界面设计注意事项
1、字体不使用固定宽度和固定大小
body{
   font:normal 100% Helvetica, Arial, sans-erif
}
字体是系统默认大小即16px;
h1 {
font-size: 1.5em;
}
h1字体大小是默认大小的1.5倍
2、适应手机屏幕宽度：
<meta name="viewpoint" content="width=device-width,initial-scale=1.0, minimum-scale=1.0, maxmum-scale=1.0 user-scalable=yes" />

2015-5-26

改进问题
1、safari界面加载报错 //未见错误
2、浏览器再次打开，到不能捕获到鼠标，暂时没有发现这个问题
3、浏览器点击别的地方也会捕获鼠标   //已解决.1146ducument.addListerner -> document.body.addListener
ting.js的函数
1、Tsing.Loader()//加载模型
  var loader = Tsing.loader();
  loader.load(url, onLoad, onProgress, onStartBuiding, onError);
2、Tsing.Viewer浏览模型
3、Tsing.Controls 操作
4、Tsing.TimeSchedul用于是吸纳timed array处理

JavaScript：
1、canvas是绘图容器，它不提供绘图方法，需要调用ctx = canvas.getContext('2d');
 ctx.fillstyle=""填充色
 ctx.fillRect=""矩形
 //draw a line
 ctx.moveTo(0,0)//startpot
 ctx.lineTo(200,200)//endspot
 ctx.stroke()
 //rotate and translate
  translate(offset_x, offset_y) //将圆点设置在新的地方，注意是按照rotate的方向建立的坐标
2、http://www.shapespark.com/仿照
css控制样式动态显示
1、鼠标悬浮：:hover
2、transition:all .5s;在未变化标签中填写
3、变换transform scale(x,y) rotate(180deg)
其他的选择器
 :link 未访问的
 :visited 已经访问的
 :active  已经激活的
 transform:scale(1.1)
指针的形状
cursor:pointer //手型


2015-5-19
1、完成cookie和session概述
2、p6
3、界面优化

cookie
 javascript对cookie的操作
  document.cookie="usename=wuwq" //cookie中不存在存在username时，就为创建一个cookie，存在时就修改
  string.indexof(str1, int1), 从int1开始的位置搜索str1，返回第一次出现的位置。
  escape(str1) 对str1比编码（为了转义）返回字符串，cookie在存储的时候进行过编码，所以在读取的时候应该解码unescape
  当用户禁止使用cookie时session就会失效，用机制session rewriting解决问题

监听全屏事件，要分别处理，a浏览器监听到了state_a=1，a做处理；b浏览器未监听到，state_b=0；如果作统一处理，state = state_a || state_b; 那么state始终是1；

jigan5353@163.com----999618pp 

2015-6-2
跟新的信息有：
1、直接覆盖
   view.php 中界面修改
   view.css 中样式修改
2、thress.js
   加入了滚轮事件
   衰减
   请求全屏事件
   鼠标拖拽事件
  
2015-6-12
javascript自定义函数
1、function fn{ var a = 1; var b = 2; alert(a + b);}
2、var fn = function(){var a = 1; var b = 2; alert(a + b);}
3、use strict //在函数中或js文件中声明，严格执行模式，所有的变量必须声明，否则报错
4、数组：方括号用逗号将元素之间隔开
   var cars = ['Saab', 'Volvo', 'BMW']; //创建数组,数组之间的元素可以使不同的类型
   var cars = new array('Saab', 'Volvo', 'BMW');
   var x = cars.length 	//property
   var y = cars.sort(); //methods, sort cars in alphabetical order
   car index = ; var fruits = ["apple", "Orange", "Apple"];  for(index = 0; index<fruits.length; index++)  console.log(fruits[index]);  //loop array element
   fruits.push("Lemon"); //add array element
 arrays use numbered indexes,objects use named indexes;
   var person = {firsName:"John", lastName:"Doe", age:46, fullname: function(c){ return this.firstName + " " + this.lastName;} }   //对象，js中没有关联数组，否则会报错
html5 video
 <video width = "300px" height = "200px" controls>
   <source src = "movie.mp4" type="video/mp4">
   <source src = "movie.ogg" type="video/ogg">
 Your browser does not support the video tag
 <video>
 说明：1、controls attribute adds video controls, like play, pause, volumn
       2、the browser will use the first recognized format
       3、Text between the <video> and </video> tags wil only display in browsers that do not support the <video> element
 其他attribute：
     1、autoplay
     2、loop
     3、muted
     4、poster:url the image displayed while the video is downloading or until the user hit the play buttons

屏幕控制:localhost/a/webgl_morphtargets_md2_control.html
domElement.addEventListener("touchstart" touchstart, false); touchend, touchmove
playerwebGL manualRotateRate 控制旋转速率
加入触屏显示移动位置


Three.js
https://github.com/mrdoob/three.js/wiki/Updates 更新

一期：
框架
界面
功能
支付

二期

2015-6-26:
1、linux命令：
   cd命令
   cd !$将上一次最后参数作为输入
   cd - 返回上一次的目录
2、图片处理：
  identify -verbose显示图片详细参数
  | grep Interlace 
  ll -h human-readable

npm
1、包安装
 npm install uglifyjs本地安装，只能在当前目录使用
 npm install -g uglifyjs全局安装
 npm uninstall uglifyjs卸载uglifyjs
 npm ls uglifyjs查看所有本地安装包
 npm -g ls uglifyjs查看所有全局安装包
 npm info uglifyjs 查看详细信息
 npm update uglifyjs 
2、在utuntu上安装会有一个小bug需要配置
 问题：/usr/bin/env: node: No such file or directory
 解决：ln -s /usr/bin/nodejs /usr/bin/node
3、uglifyjs压缩和替换变量名 
  uplifyjs Tsing.js -m -o Tsing.min.ugly.js
\\HPWORKSTATION\ShareData\BACKUP\15.6.30-view\js\Tsing.js

查看文件和系统磁盘大小
df -h 查看系统所有文件系统的可用空间和实用情况
du -h --max-depth=1 * 查看该目录下所有文件夹和文件的大小
ll -h 中可以查看到文件的大小

webm格式是高质量的视屏压缩格式 配合H5格式
krpano文件：
actions/scripting 动态语言
XML Reference static XML code

js
移动：
 scrollTo(xpos,ypos);移动到某位置
 scrollBy(xnum,ynum);移动多少距离
取整：
 parseInt()：保留整数部分
 Math.cell():向上取整
 Math.floor():向下取整
 Math.round():四舍五入

jquery
动态修改高度
.box{ width:100px; height:200px; 17:41 2015/7/14 transition-property: height, opacity}
.hidden{height:0; opacity:0}
$('button').on("click" ,function(){box.toggleClsss("hidden")});
让页面跳转到顶部
$('html,body').animate({scrollTop:0}, 1000);
元素缓慢消失，出现
$('element').fadeIn();
$("element").fadeOut();

css：
selector：
 id:#nav{} element:body{} class:.nav{}
 to group selector : h1,h2,p{}
 类选择器：结合元素选择器 p.important{} 多类选择器".important.warning" 中间没有空格
 属性选择器：a[href]{} 对包含href属性的a标签应用样式
 后代选择器：使用空格。
 ">" 选择子元素 h1 > strong
 相邻兄弟选择器：h1 + p 选择紧接在h1后出现的p

2015-7-21
新建wordpress论坛
1、修改登录界面：使用插件：ludou-custom-register，并在里面修改
2、路径名称使用英文
修改 wordpress功能小工具
 wp-include:default-widgets.php 修改
3、plugins
  使用绝对路径：plugin_dir_path plugins_urls
  防止直接访问：加 defined("ABSPATH") or die("something");
  数据库
    post_meta add_post_meta
  加载脚本
    script:wp_enqueue_script()
    style:wp_enqueue_style()   
  写插件,开启debug模式
    在wp_config.php加入define("DEBUG", true)

/etc/hosts文件地址
apache2配置文件 二级域名
sites-available: 存放配置文件
sites-enable: 有到sites-available的软连接时文件生效 可以使用ln -s existingfile newfile 创建软连接 或者使用命令a2ensite(打开) a2idissite 
apachectl

配置某个文件禁止被访问
 <Files Tsing.js>是限制某个文件夹下某文件禁止被访问
   Order deny,allow
   Deny from all
 </Files>

问题：jquery和js混用？

    
bootstrap
data-toggle:相当于调用那个js模块，与下面两者之一配合使用
data-target:相当于显示那个
href:与上同

数据库
var model = api.require('model')；
var query = api.require('query')
model.config({})
query.createQuery({
},function(ret,err){
}
user 对象：
user.regster
user.login()

localstorage
localStorage.setItem('userId', ret.id);

model.findfall()


Laravel使用：
路由：app/Http/routers.php
控制器：php artisan make:controller Admin/AdminHomecontroller
        得到 app/Http/Controller/Admin/AdminHomeController.php
中文字体使用说明
1、font-family中文名称和英文字体名称都要有，还有就是中文字体和英文字体都有的情况下，先写英文，在写中文
font-family: STXihei, "华文细黑", "Microsoft YaHei", "微软雅黑";


数据库创建的时候：
自动时间：time timestamp not null default current_timestamp
create table user()enging=innodb default charset=utf8


redhat安装lamp
yum mysql 增加用户
yum install mysql mysql-server 
mysql_secure_installation
insert into user(Host, user, password) values("localhost", "wuwq", password("password"));
grant all privileges on *.* to wuwq@"localhost"
flush privileges;

nginx配置
  main
    event 
    http
        server
	location

css flex
flex-container
  display:flex;
  flex-direction: row | row-reverse | column | column-reverse 定义伸缩方向 
  flex-wrap: nowrap | wrap |wrap-reverse 是否是同一行	
  flex-flow: 是flex-direction和flex-wrap的合体 default row nowrap
  justify-content：flex-start| flex-end | center | space-between | space-around 水平布局
  align-items:flex-start | flex-end | stetch | center | baseline 每一行的对其方式
  align-content	:flex-start| flex-end | center | space-between | space-around 垂直布局
item
  order:integer
  flex-grow:
  flex-shrink
  flex-basis:0 auto是否自动分配剩余空间
  flex是flex-grow flex-shrink flex-basis
  align-self在align-item中单独设置
  



庞欣的12306
1146294155@qq.com
pangxin1146294155

https://item.taobao.com/item.htm?spm=a230r.1.14.1.RPWEED&id=520172120615&ns=1&abbucket=10#detail

http://mp.weixin.qq.com/wiki/7/2d301d4b757dedc333b9a9854b457b47.html

359194
用到的函数：chunk_split(story,13);

nginx配置主机文件的路劲/alidata/server/nginx/conf/vhosts/*.conf;

chrome查看密码：
chrome://settings/passwords



js学习：

5种基本数据类型：undefined 未赋值 ,null（）, Boolean, Number（整数或浮点）, String。 复制存储的是变量的值，所以参数传递的时候不影响外部变量值
引用数据类型：object, function, array， 复制存储的是对象的应用，所以传递的参数就是引用的值，会影响到外部变量的值

相同点：声明方式一样：var a = 


全局变量定义在global（node）和windows（浏览器）
变量的作用域是函数级别的



微信支付的问题：
1、商户订单号的问题比较大，
   首字母不能为数字
   订单如果支付成功了，就不能发起新的订单，就是商户订单号唯一性


建立git服务器
1、安装git
sudo apt-get install git
2、创建git用户
sudo adduser git
3、创建证书登录
vi /home/git/,ssh/authorized_keys
添加public_key
4、建立git仓库
sudo git init --bare name.git
sudo chown -R git:git name.git
5、禁用shell登录
git:x:1001:1001:,,,:/home/git:/bin/bash
改为git:x:1001:1001:,,,:/home/git:/usr/bin/git-shell
6、克隆远程仓库：
git clone git@servername:/home/git/name.git



apachectl: 控制apache http的程序（以后可能不在支持）
httpd 控制 /usr/sbin/httpd 
composer /usr/local/bin/composer
laravel ['lærəvel]

api 访问
api http://h5app.7dyk.com/ama/api/public/api/v1/question/mylisten?page=1&number=10
音频 http://h5app.7dyk.com/ama/api/public/api/v1/answer/audio?name=3cfeaa89e9355868494673143f0006



php 的包管理工具 composer  https://packagist.org 包含有使用composer安装的php包

1、创建项目：
composer create-project laravel/lavaral learnlaravel5
2、 配置权限
sudo chmod -R 777 learnlaravel5/storage
3、数据库配置
vi learnlaravel5/.env
4、数据库迁移
php artisan migrate 美[ˈɑ:rtəzn] 技工














